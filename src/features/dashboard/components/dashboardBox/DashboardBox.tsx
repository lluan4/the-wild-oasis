import * as S from '../../styles/DashboardBox.styles';

function DashboardBox({ children }: { children: React.ReactNode }) {
  return <S.DashboardBox>{children}</S.DashboardBox>;
}

export default DashboardBox;
